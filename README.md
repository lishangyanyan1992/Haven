# Haven Monorepo

This repository contains multiple Haven products:

- `apps/haven`: the main Haven application
- `apps/immig-wizard`: the standalone immigration filing wizard
- `packages/auth`: shared Supabase auth helpers

Shared backend infrastructure remains at the repo root under `supabase/` and `scripts/`.
